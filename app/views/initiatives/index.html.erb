<% title t('initiatives.initiatives') %>

<h2 class="section-header"><%= t('initiatives.initiatives') %></h2>

<%= render "search_form" %>

<div class="sorting row">
  <%= link_to t('initiatives.most_viewed'), initiatives_path(order: "views_count"), class: "sort button" %>
  <%= link_to t('initiatives.most_recent'), initiatives_path(order: "presented_at"), class: "sort button"  %>
</div>

<div class="initiatives">
  <%= render @initiatives %>
</div>

<%= paginate @initiatives %>